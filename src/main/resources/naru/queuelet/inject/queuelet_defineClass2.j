private Class queuelet_defineClass2(String name,
									  byte[] b,
									  int off,
									  int len,
									  java.security.ProtectionDomain protectionDomain)
							   throws ClassFormatError{
	/* protectionDomain ignore fix me */
	return queuelet_defineClass(name,b,off,len);
}
